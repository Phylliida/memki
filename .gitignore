dev/
Memki.apk
oss-anki.apk
Memki.ipa
# Release signing key — NEVER committed (CI gets it via Actions secrets).
# The committed android/app/anki-debug.keystore is a public DEBUG key, fine.
*.keystore
!android/app/anki-debug.keystore
save/
node_modules/
.direnv/
*.log
.DS_Store
dist/
tmp/
# Prebuilt binary from the pinned npm sql.js package — restored by postinstall
# (scripts/copy-sqljs-wasm.js), never committed (F-Droid scans for blobs).
vendor/sqljs/sql-wasm.wasm
ios/
web/.file-server-state.json
__pycache__/
